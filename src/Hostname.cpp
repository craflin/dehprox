
#include "Hostname.h"

bool Hostname::resolve(const String& name, uint32& addr, uint16& port)
{
    // todo
    return false;
}

bool Hostname::reverseResolveFake(uint32 addr, uint16 port, const String& name)
{
    // todo
    return false;
}

bool Hostname::reverseResolve(uint32 addr, uint16 port, const String& name)
{
    // todo
    return false;
}
